networks:
  net_test_testproject:
    driver: bridge
services:
  app:
    environment: {}
    image: nginx:alpine
    networks:
    - net_test_testproject
    ports:
    - 8081:80
    restart: unless-stopped
    volumes:
    - /app/clients/test/testproject/source:/var/www/html
version: '3.8'
