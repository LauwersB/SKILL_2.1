services:
  platform-api:
    build: ./Platform-api
    ports:
      - "8080:8080"
    environment:
      - PLATFORM_DB_HOST=platform-db
      - PLATFORM_DB_USER=platform
      - PLATFORM_DB_PASSWORD=platform123
      - PLATFORM_DB_NAME=platform
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # toegang tot Docker Engine
      - ./staging:/app/staging  # Mount staging directory voor applicatiebestanden
      - ./staging:/tmp/poc-deployments  # Mount voor ingest_app.sh script
    depends_on:
      - platform-db

  platform-db:
    image: postgres:16
    environment:
      - POSTGRES_DB=platform
      - POSTGRES_USER=platform
      - POSTGRES_PASSWORD=platform123
    ports:
      - "5433:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

